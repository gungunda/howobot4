# 📘 Контекст проекта «Лёшин планировщик домашних заданий»
_Свод всех ключевых правил и уточнений из текущего чата (версия 2025‑11‑03)_

---

## ⚙️ Общие принципы проекта

**Цель:** минималистичное приложение‑планировщик домашних заданий для ученика 8‑го класса.  
Работает как **Telegram WebApp**, данные сохраняются в **LocalStorage**, без сторонних фреймворков.  
Архитектура — **Clean Architecture** с разделением на:
- entities
- usecases
- storage
- ui (view/presenter)
- utils

---

## 🧱 Архитектурная логика данных

### Основные сущности
- **Schedule** — недельное расписание (7 массивов задач).
- **DayTasks** — дневные оверрайды (сохраняются как day_YYYY‑MM‑DD).
- **Task** — отдельная задача (id, title, minutes, progress, closed, unloadDays).

---

## 🗓️ Логика дней, расписания и разгрузки

### Термины
- **R** — «родной» день задачи в расписании (день, где она стоит по расписанию).
- **D** — день выполнения («канун» R, то есть D = R − 1).
- **W** — текущий день, отображаемый на дашборде.
- **T** — ближайшая календарная дата для дня (R−1), если редактируем задачу из разгрузки.

### Общие принципы
1. На **дашборде в день D** отображаются задачи из **расписания на D+1 (R)**.  
   Изменения (прогресс, минуты, закрытие) сохраняются в **DayTasks(D)**.
2. Разгрузка (`offload`) показывает задачи из других дней R, где в `unloadDays` указан день D.
3. При правке задачи в разгрузке изменения пишутся в **DayTasks(T)**, где T — ближайшая дата дня (R−1).
4. Если задача уже присутствует в `DayTasks(T)`, данные берутся оттуда, а не из расписания.
5. Если в `unloadDays` указан день (R−1), он игнорируется — этот день уже является основным кануном.

### Пример
- В расписании у «Математики (Сб)» стоит `unloadDays = [5]` (Пт).  
  → В пятницу эта задача появится в «Разгрузке».  
  При изменении прогресса в пятницу запись попадёт в `DayTasks(Пт)`.

---

## 💾 Правила сохранения (фикс от 2025‑11‑03)

- Основные задачи дашборда сохраняются в **DayTasks(D)**, где D = (D+1) − 1.  
- Разгрузка сохраняет прогресс в **DayTasks(targetIso)** (канун дня R).  
- При первом касании задачи `ensureDayTask()` подтягивает данные из расписания **R = D+1**, чтобы не терялись `title` и `minutes`.

---

## 🧩 Правила именования переменных и функций

1. **Длина:** имена из 1–2 слогов, максимум 3.  
   Пример: `makeNextDay`, `getDayPlan`.
2. **Смысл:** имя отвечает на вопрос «что делает», не «как делает».  
   Пример: `getDayPlan`, не `loadScheduleForDate`.
3. **Контекст:** не повторять часть, уже заданную именем модуля.  
   Пример: в `storage.js` → `loadSchedule`, не `loadScheduleFromStorage`.
4. **Предсказуемость:** базовый словарь:  
   `load`, `save`, `make`, `get`, `set`, `show`, `hide`, `add`, `drop`, `plan`, `task`, `day`, `week`, `view`, `list`, `name`, `time`.
5. **Согласование:** если есть сомнения — ассистент предлагает 2–3 коротких варианта и ждёт подтверждения пользователя.

---

## 💬 Правила комментирования и форматирования кода

1. **Комментарии обязательны для каждой функции.**
   - Используется `/** ... */` с описанием, параметрами (`@param`), возвращаемым значением (`@returns`).
   - Язык комментариев — русский.

2. **Однострочные комментарии.**
   - Для пояснений внутри тела — `//`, коротко и по сути.
   - Каждый логический блок сопровождается комментарием.

3. **Форматирование.**
   - Отступы — 2 пробела.
   - Между блоками — пустая строка.
   - Фигурные скобки открываются на той же строке (`if (...) {`).

4. **Шапка файла.**
   - Каждый JS‑файл начинается с `/** Назначение файла, где используется. */`.

5. **Читаемость.**
   - Максимум 100 символов в строке.
   - Имена по правилу лаконичных имён.
   - Без непонятных аббревиатур.

6. **Единый стиль.**
   - Комментарии поясняют логику, не историю правок.
   - Не противоречат друг другу.

---

## 🔒 Правила дисциплины изменений

1. Никаких изменений без предварительного плана и одобрения пользователя.  
2. Перед каждой правкой ассистент предоставляет список файлов и описание предполагаемых действий.  
3. Если затрагиваются экспорты, архитектурные функции или интерфейсы между модулями — требуется отдельное подтверждение.  
4. Перед началом работы ассистент сверяется с `Правила_взаимодействия.md`.  
5. Любое несоответствие правилам блокирует генерацию патча до согласования.

---

## 🧠 Внутренние принципы работы ассистента

- Всегда сначала запрашивает список нужных файлов.  
- Работает только с утверждёнными бандлами.  
- Генерирует ответы в формате FILE_INDEX‑бандлов, не выводя код на экран.  
- При сомнении в названии, архитектуре или интерфейсе — делает паузу и запрашивает подтверждение.

---

## 🎨 Интерфейс и структура экранов

### Основные экраны
- **Календарь** — выбор даты, отображение сетки дней, переход на дашборд по клику.
- **Дашборд** — задачи на D+1 (основные), блок «Разгрузка», статистика дня.
- **Расписание (Schedule)** — редактирование недельного шаблона, установка `unloadDays`.

### Блок «Разгрузка»
- Заголовок, минуты (`hh:mm`), метка «из дня: Чт`.
- Ползунок и кнопки ±10%, изменяющие `DayTasks(T)`.
- Отображается только если список оффлоад‑задач не пуст.

---

## 📅 Текущее состояние логики (на момент 2025‑11‑03)

✅ Календарь работает — клики по датам переключают дашборд.  
✅ Основные задачи дашборда сохраняются в `DayTasks(D)` корректно.  
✅ Разгрузка отображает только разрешённые дни (по `unloadDays`).  
✅ Прогресс разгрузки сохраняется в правильный `DayTasks(T)`.  
🛠️ Следующий этап — переход к **доработке дизайна интерфейса**.

---

_Этот файл содержит полный контекст чата для продолжения работы над проектом в новом потоке._
