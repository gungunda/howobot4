# Контекст проекта «Лёшин планировщик домашних заданий»

## 🧭 Общие данные
- **Цель:** Создать минималистичное приложение-планировщик домашних заданий для ученика 8-го класса, работающее как Telegram WebApp.
- **Технологии:** HTML5 + CSS3 + JavaScript (ES6+), без TypeScript и фреймворков.
- **Среда:** Telegram WebApp (встроенный браузер Telegram).
- **Хранилище:** LocalStorage (облако Telegram пока не используется).
- **Архитектура:** Clean Architecture — `entities`, `usecases`, `storage`, `ui`.

---

## ⚙️ Правила взаимодействия и работы с проектом
1. Перед началом работы я всегда присылаю список нужных файлов (пути от корня).
2. Перед изменениями я всегда составляю план (какие файлы и функции будут затронуты) и жду явного согласования.
3. Я не вношу улучшений без запроса.
4. Изменения в экспортируемых функциях или интерфейсах требуют дополнительного подтверждения.
5. Перед генерацией патча я обязан убедиться, что:
   - есть утверждённый план,
   - есть список файлов,
   - есть согласие на архитектурные изменения.
   Если чего-то не хватает — я останавливаю работу и запрашиваю подтверждение.

---

## 🧩 Формат работы с файлами и патчами
- Все изменения передаются через текстовый **бандл** (`FILE_INDEX` + `// /path` секции).
- Каждый файл в бандле содержится **полностью**, в актуальном виде.
- Патчи не выводятся на экран, а передаются в виде текстового файла для загрузки.
- Я никогда не добавляю фразы вроде «оставить остальное без изменений».
- Формат строго сохраняется, чтобы `apply-patch.ps1` мог безопасно применять правки.

---

## ⚠️ ОБЯЗАТЕЛЬНОЕ ПРАВИЛО ПЕРЕДАЧИ ФАЙЛОВ В БАНДЛАХ

> **Каждый файл в бандле всегда передаётся целиком.**
> Ни при каких обстоятельствах нельзя отправлять частичный контент, вырезку, вставку, дифф, текст «оставить остальное без изменений» и тому подобное.
>
> Если в файле были изменения — в бандл включается **полный актуальный текст всего файла после изменений**.
>
> ❌ Нельзя:
> - писать «остальное без изменений»;
> - передавать только изменённый участок;
> - удалять несвязанные участки кода ради краткости.
>
> ✅ Нужно:
> - передавать полный файл, в его итоговом виде, с актуальной структурой и комментариями;
> - соблюдать исходный формат бандла (FILE_INDEX и // /path секции).
>
> Это гарантирует, что при применении бандла через `apply-patch.ps1` ни один файл не будет повреждён или обрезан.

---

## 💬 Правила именования и форматирования

### Именование (правило лаконичных имён)
1. **Длина:** имена из 1–2 слогов, максимум 3.
2. **Смысл:** имя отвечает на вопрос *«что делает»*, а не *«как делает»*.
3. **Контекст:** не повторять часть, уже заданную именем файла или модуля.
4. **Предсказуемость:** базовые глаголы и существительные: `load`, `save`, `make`, `get`, `set`, `show`, `hide`, `add`, `drop`, `plan`, `task`, `day`, `week`, `view`, `list`, `name`, `time`.
5. При сомнении я предлагаю 2–3 коротких варианта и согласовываю с пользователем.

### Комментирование и форматирование
1. Каждый JS-файл начинается с шапки `/** Назначение файла... */`.
2. Каждая функция сопровождается JSDoc-комментарием (`/** ... @param @returns */`).
3. Внутри функций — короткие `//`-комментарии на русском, поясняющие логику.
4. Отступы — 2 пробела, фигурные скобки на той же строке.
5. Максимальная длина строки — 100 символов.
6. Единый стиль комментариев по всему проекту.
7. Комментарии служат для пояснения логики, а не как история правок.

---

## 🔁 Правила логики данных и дат (D / D+1 / R / T)
- На **дашборде сегодня (D)** показываются задачи на **завтра (D+1)**.
- Все изменения (`progress`, `minutes`, `closed`) сохраняются в **DayTasks(D)**, то есть в «сегодня».
- Разгрузка работает по правилу:
  если предмет X из расписания дня R имеет `unloadDays`, включающий день недели W,
  то он отображается в **разгрузке дня W**, а изменения вносятся в **DayTasks(R-1)**.
- Таким образом:
  - Основные задачи → `DayTasks(D)`
  - Разгрузка → `DayTasks(R-1)` (канун «родного» дня).

---

## 🧱 Архитектурные принципы
- **Entities:** `Task`, `Schedule`, `DayTasks` и др. — только бизнес-логика.
- **Usecases:** операции вроде `LoadDashboard`, `SetProgress`, `AddDayTask` и т.д.
- **Storage:** интерфейс доступа к LocalStorage (без бизнес-логики).
- **UI:** отображение (`view.js`), связь с презентерами (`presenter.js`).
- **Без сторонних фреймворков**, только чистый JS и DOM API.
- Разделение ответственности строго соблюдается.

---

## 📄 Рабочий процесс
1. Я присылаю список нужных файлов (пути от корня).
2. Пользователь присылает бандл с актуальными текстами этих файлов.
3. Я вношу согласованные правки и возвращаю **текстовый бандл** с полными файлами.
4. Применение происходит через `apply-patch.ps1`.
5. После проверки — фиксируем и идём к следующему шагу.
